import { css } from '../../../styled-system/css'

export const mainContainer = css({
  width: 'full',
  height: 'calc(100% - 60px)', // TODO: improve header height handling
  padding: '12px',
  overflow: 'auto',
})
